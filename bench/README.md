Benchmark Suite (v0.3.2.9.2)

- Files prefixed with `runtime_` are intended to be compiled and executed.
- Files prefixed with `compile_` are intended to be compiled for compile-time/memory profiling.
- All programs are deterministic and should exit with code 0 on success.

Run:

  python3 scripts/bench.py --mode all
