صحيح مصيدة = 0.

صحيح الرئيسية() {
    صحيح س = 1.
    صحيح ع = 0.

    طالما (ع < 2000000) {
        س = س + (ع * 3).
        س = س - (س / 7).
        إذا (س < 0) {
            س = -س.
        }
        ع = ع + 1.
    }

    مصيدة = س.
    إرجع 0.
}
