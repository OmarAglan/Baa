# نظرة عامة على معمارية مترجم لغة باء

---
**الحالة:** البنية التحتية الأساسية جاهزة للإنتاج ✅ | التحليل الدلالي وتوليد الكود مخطط 📋
**آخر تحديث:** 2025-11-06
**الإصدار:** v0.1.15+
**حالة الأولوية 4:** مكتملة (المعالج المسبق، المحلل اللفظي، المحلل النحوي، شجرة النحو المجردة مع دعم الدوال)

---

## نظرة عامة

لغة باء (باء) مصممة بمعمارية معيارية تفصل الاهتمامات في مكونات متميزة. كل مكون مسؤول عن جانب محدد من عملية التجميع، مما يجعل النظام قابلاً للصيانة والتوسع. **اعتباراً من 2025-11-06، البنية التحتية الأساسية للمترجم جاهزة للإنتاج مع اكتمال الأولوية 4 (تعريفات الدوال واستدعاءاتها).**

## الفلسفة التصميمية

مترجم لغة باء مصمم وفقاً لمبادئ المترجمات الحديثة مع التركيز على:

- **الوضوح والبساطة**: كود واضح وقابل للقراءة
- **القابلية للصيانة**: معمارية معيارية مع واجهات محددة بوضوح
- **دعم اللغة العربية**: تكامل أصيل للغة العربية في جميع المستويات
- **التوافق مع C**: الحفاظ على المفاهيم المألوفة مع إضافة النحو العربي
- **الأداء**: تحسينات مناسبة بدون تعقيد مفرط

## نظرة عامة على المراحل

مترجم باء يتبع مراحل التجميع الكلاسيكية مع تحسينات للدعم العربي:

```
ملف المصدر باء (.ب)
        ↓
    [معالج مسبق]     ← توجيهات عربية (#تضمين، #تعريف، #إذا)
        ↓
     تدفق UTF-16
        ↓
    [محلل لفظي]      ← كلمات مفتاحية عربية، أرقام عربية-هندية
        ↓
     تدفق الرموز
        ↓
    [محلل نحوي]      ← نحو عربي، تحليل نزولي تكراري
        ↓
   شجرة نحو مجردة
        ↓
   [محلل دلالي]      ← فحص الأنواع، حل الرموز (مخطط)
        ↓
 شجرة نحو محققة
        ↓
   [مولد الكود]      ← LLVM IR (مخطط)
        ↓
    كود الهدف
```

## المكونات الرئيسية

### 0. المعالج المسبق (المعالج المسبق) ✅ جاهز للإنتاج

المرحلة الأولية التي تعالج ملف المصدر قبل التحليل اللفظي:

- **معالجة التوجيهات**: معالجة توجيهات مثل `#تضمين` (include) و `#تعريف` (define).
- **تضمين الملفات**: دمج الملفات المضمنة في تدفق واحد.
- **توسيع الماكرو**: استبدال الماكروات الشبيهة بالكائنات والشبيهة بالدوال المعرفة. يدعم التحويل إلى سلسلة نصية (`#`) ولصق الرموز (`##`). يكتشف التوسع التكراري.
- **ترميز الإدخال**: يتوقع ملفات إدخال UTF-16LE (يتحقق من BOM)، أو UTF-8 (مع/بدون BOM، يتم الكشف التلقائي والتحويل إلى UTF-16LE داخلياً).
- **الإخراج**: ينتج تدفق `wchar_t*` واحد (UTF-16LE) للمحلل اللفظي.
- **كشف التضمين الدائري**: يمنع حلقات التضمين اللانهائية.
- **التجميع الشرطي**: يتعامل مع `#إذا`، `#إذا_عرف`، `#إذا_لم_يعرف`، `#وإلا_إذا`، `#إلا`، `#نهاية_إذا`. يقيم تعبيرات عددية ثابتة لـ `#إذا`/`#وإلا_إذا` (يدعم العمليات الحسابية، المقارنة، المنطقية، البتية، `معرف()`).
- **الإبلاغ عن الأخطاء**: يبلغ عن الأخطاء مع سياق مسار الملف ورقم السطر. يجمع عدة تشخيصات.
- **الميزات والحالة:**
  - منفذ كمرحلة منفصلة.
  - يتعامل مع `#تضمين` (مسارات نسبية وقياسية).
  - يتعامل مع `#تعريف` (ماكروات شبيهة بالكائنات والدوال، بما في ذلك `#`، `##`، variadic، و**إعادة فحص نتائج التوسع**).
  - يتعامل مع `#الغاء_تعريف` لإلغاء تعريف الماكروات.
  - يتعامل مع التجميع الشرطي (`#إذا_عرف`، `#إذا_لم_يعرف`، `#إلا`، `#نهاية_إذا`، `#إذا`، `#وإلا_إذا`) مع تقييم التعبيرات (باستخدام `معرف` لـ `defined`).
  - يدعم توجيهات `#خطأ` و `#تحذير`.
  - يكتشف التضمينات الدائرية وتوسع الماكرو التكراري.
  - يوفر ماكروات محددة مسبقاً (`__الملف__`، `__السطر__` (int)، `__التاريخ__`، `__الوقت__`، `__الدالة__` (نائب)، `__إصدار_المعيار_باء__`).
  - *راجع [`docs/04_ROADMAP/PREPROCESSOR_ROADMAP.md`](../04_ROADMAP/PREPROCESSOR_ROADMAP.md) لأحدث الحالة، بما في ذلك تحسينات استرداد الأخطاء.*

### 1. المحلل اللفظي ✅ جاهز للإنتاج

المحلل اللفظي المسؤول عن تحليل كود المصدر إلى رموز. له هيكل معياري:

- [`lexer.c`](../../src/lexer/lexer.c): منطق التوزيع الأساسي (`baa_lexer_next_token`) ودوال مساعدة.
- [`token_scanners.c`](../../src/lexer/token_scanners.c): ينفذ دوال مسح محددة للمعرفات، الأرقام، السلاسل النصية، والتعليقات.
- [`lexer_char_utils.c`](../../src/lexer/lexer_char_utils.c): يوفر أدوات تصنيف الأحرف (مثل للأحرف العربية، الأرقام).
- **توليد الرموز**: تحويل نص المصدر (إخراج UTF-16LE من المعالج المسبق) إلى تدفق من الرموز.
- **دعم Unicode**: دعم كامل للأحرف العربية في المعرفات، القيم الحرفية، والكلمات المفتاحية. يتعرف على الأرقام العربية-الهندية.
- **تتبع المصدر**: تتبع دقيق للسطر والعمود للرموز والأخطاء.
- **معالجة الأخطاء المحسنة**: نظام شامل للكشف عن الأخطاء والإبلاغ عنها مع:
  - **أنواع أخطاء محددة**: 8 أنواع رموز خطأ متميزة (سلاسل غير منتهية، تسلسلات هروب غير صالحة، أحرف غير منتهية، أرقام/لواحق/تعليقات غير صالحة)
  - **رموز الأخطاء**: رموز فريدة (1001-1009، 9001) للترجمة والتصنيف
  - **رسائل عربية**: جميع رسائل الخطأ والاقتراحات مقدمة بالعربية
  - **سياق غني**: فئات الأخطاء، اقتراحات مفيدة، وتتبع دقيق لموقع المصدر
  - **استرداد الأخطاء**: آليات مزامنة قوية لمتابعة التحليل بعد الأخطاء
- **المسافات البيضاء والأسطر الجديدة**: تحليل المسافات/علامات التبويب (`BAA_TOKEN_WHITESPACE`) والأسطر الجديدة (`BAA_TOKEN_NEWLINE`) بشكل منفصل.
- **معالجة التعليقات**: تحليل تعليقات السطر الواحد (`//` -> `BAA_TOKEN_SINGLE_LINE_COMMENT`)، متعددة الأسطر (`/*...*/` -> `BAA_TOKEN_MULTI_LINE_COMMENT`)، والتوثيقية (`/**...*/` -> `BAA_TOKEN_DOC_COMMENT`). محتوى التعليقات يحتوي على النص باستثناء المحددات.
- **التعرف على القيم الحرفية العددية**:
  - يحدد تنسيقات الأرقام المختلفة: الأعداد الصحيحة (عشري، ثنائي `0b`/`0B`، سداسي عشري `0x`/`0X`)، الأعداد العشرية (باستخدام `.` أو `٫` كفاصل عشري).
  - يستخدم علامة الأس العربية `أ` للترميز العلمي (لا يدعم `e`/`E` الإنجليزية) - منفذ بالكامل وعامل.
  - يدعم الأرقام العربية-الهندية (`٠`-`٩`) والأرقام الغربية (`0`-`9`) في جميع أجزاء الأرقام.
  - يدعم الشرطات السفلية (`_`) كفواصل لتحسين القراءة في الأرقام.
  - يحلل لواحق القيم الحرفية الصحيحة العربية (`غ`، `ط`، `طط`) ولاحقة العشرية (`ح`) - منفذ بالكامل وعامل.
  - دالة `scan_number` في المحلل اللفظي تتعامل مع التعرف النحوي وتستخرج المحتوى الخام. أداة منفصلة [`number_parser.c`](../../src/lexer/number_parser.c) تحول هذه المحتويات إلى قيم عددية فعلية.
- **القيم الحرفية للسلاسل النصية/الأحرف**: يتعامل مع السلاسل النصية (`"..."`، `"""..."""` متعددة الأسطر، `خ"..."` خام) والأحرف (`'...'`).
  - معالجة **تسلسلات الهروب العربية الخاصة بباء** (مثل `\س` للسطر الجديد، `\م` للتبويب، `\يXXXX` لـ Unicode، `\هـHH` للبايت السداسي عشري) - منفذ بالكامل وعامل في جميع أنواع السلاسل النصية.
  - تسلسلات الهروب القياسية C مثل `\n`، `\t`، `\uXXXX` **غير** مدعومة وستؤدي إلى أخطاء.
- **الميزات والحالة:**
  - وظائف المحلل اللفظي الأساسية والهيكل المعياري منفذة.
  - معالجة قوية للرموز للكلمات المفتاحية، المعرفات، العمليات، وأنواع القيم الحرفية المختلفة.
  - دعم القيم الحرفية للسلاسل النصية والأحرف مع تسلسلات الهروب الخاصة بباء.
  - تتبع موقع المصدر.
  - توليد رموز الأخطاء.
  - *راجع [`docs/04_ROADMAP/LEXER_ROADMAP.md`](../04_ROADMAP/LEXER_ROADMAP.md) للحالة التفصيلية والتحسينات المخططة.*

### 2. المحلل النحوي ✅ جاهز للإنتاج

يحول الرموز إلى شجرة نحو مجردة منظمة:

- **الحالة**: جاهز للإنتاج مع اكتمال الأولوية 4 (v0.1.15+). تعريفات الدوال، استدعاءات الدوال، والتراكيب اللغوية الأساسية منفذة بالكامل.
- **النهج**: هبوط تكراري.
- **الإخراج**: شجرة نحو مجردة (AST) مكونة من `BaaNode`s.
- **معالجة الأخطاء**: كشف الأخطاء النحوية، الإبلاغ عنها، واسترداد وضع الذعر.
- **الميزات المكتملة**: إعلانات المتغيرات، التعبيرات (القيم الحرفية، المعرفات، الثنائية، الأحادية، الاستدعاءات)، الجمل (إذا، طالما، لكل، إرجع، كتلة)، تعريفات الدوال مع المعاملات.
- *راجع [`docs/02_COMPILER_ARCHITECTURE/PARSER.md`](PARSER.md) و [`docs/04_ROADMAP/PARSER_ROADMAP.md`](../04_ROADMAP/PARSER_ROADMAP.md) للتفاصيل.*

### 3. شجرة النحو المجردة (AST) ✅ جاهزة للإنتاج

توفر وحدة AST الأساس لتمثيل هيكل الكود باستخدام نهج `BaaNode` موحد.

- **الحالة**: جاهزة للإنتاج مع اكتمال الأولوية 4 (v0.1.15+). جميع أنواع العقد الأساسية منفذة للتعبيرات، الجمل، الإعلانات، والدوال.
- **الهيكل**:
  - `BaaNode` موحد مع `BaaNodeKind kind`، `BaaSourceSpan span`، و `void* data`.
  - هياكل بيانات محددة (مثل `BaaLiteralExprData`، `BaaFunctionDefData`، `BaaCallExprData`) لكل نوع عقدة.
- **إدارة الذاكرة**: `baa_ast_new_node()` للإنشاء الأساسي، `baa_ast_free_node()` للتحرير التكراري.
- **الاجتياز**: نمط الزائر مخطط لاجتياز AST.
- **أنواع العقد المكتملة**: البرنامج، تعريفات الدوال، المعاملات، إعلانات المتغيرات، التعبيرات (حرفي، معرف، ثنائي، أحادي، استدعاء، إسناد)، الجمل (كتلة، إذا، طالما، لكل، إرجع، تعبير).
- *راجع [`docs/02_COMPILER_ARCHITECTURE/AST.md`](AST.md) و [`docs/04_ROADMAP/AST_ROADMAP.md`](../04_ROADMAP/AST_ROADMAP.md) للتفاصيل.*

### 4. نظام الأنواع

نظام أنواع قوي يدعم التوافق مع C وأسماء الأنواع العربية:

- **الأنواع الأساسية**: `عدد_صحيح` (int)، `عدد_حقيقي` (float)، `حرف` (char)، `منطقي` (bool)، `فراغ` (void).
- **فحص الأنواع**: فحص الأنواع الثابت (مخطط للتحليل الدلالي).
- **تحويل الأنواع**: قواعد التحويل الضمني والصريح للأنواع (مخطط للتحليل الدلالي).
- **الميزات والحالة:**
  - تعريفات الأنواع الأساسية وهياكلها منفذة.
  - هيكل نوع المصفوفة (`BAA_TYPE_ARRAY`) معرّف.

### 5. العمليات

نظام عمليات مع دعم كامل للعربية:

- **الحسابية**: `+`، `-`، `*`، `/`، `%`
- **المقارنة**: `==`، `!=`، `>`، `<`، `>=`، `<=`
- **المنطقية**: يستخدم الرموز `&&` (و - AND)، `||` (أو - OR)، `!` (ليس - NOT).
- **الأولوية**: قواعد أولوية واضحة للعمليات معرفة.
- **الميزات والحالة:**
  - تعريفات العمليات، الرموز، والأولوية منفذة.
  - بدايات التحقق الأساسي موجودة.

### 6. التحكم في التدفق (الدلالي وتمثيل AST)

تراكيب التحكم مع الكلمات المفتاحية العربية:

- **الشروط**: `إذا` (if)، `وإلا` (else)
- **الحلقات**: `طالما` (while)، `لكل` (for)
- **الدوال**: (يستخدم إعلانات بأسلوب C)
- **الإرجاع**: `إرجع` (return)
- **الحالة**: الكلمات المفتاحية محللة بواسطة المحلل اللفظي. قواعد المحلل النحوي وتعريفات عقد AST جزء من تصميم المحلل النحوي/AST الجديد (جارية). التحقق الدلالي مخطط.

### 7. الأدوات

دوال الأدوات وميزات الدعم:

- **معالجة الأخطاء**: تعدادات الأخطاء الأساسية ودوال الإبلاغ.
- **إدارة الذاكرة**: أغلفة تخصيص ذاكرة آمنة (`baa_malloc`، `baa_free`، `baa_strdup`).
- **معالجة السلاسل النصية**: عمليات سلاسل الأحرف الواسعة UTF-16LE.
- **عمليات الملفات**: معالجة ملفات المصدر مع الكشف التلقائي UTF-8/UTF-16LE (في المعالج المسبق) ومعالجة UTF-16LE (في المحلل اللفظي).
- **الميزات والحالة:** منفذة.

### 8. التحليل الدلالي 📋 مخطط

مسؤول عن التحقق من الدلاليات الثابتة، حل الأسماء، وفحص الأنواع.

- **الحالة**: مخطط للتنفيذ المستقبلي. هيكل تحليل التدفق الأساسي موجود ([`src/analysis/`](../../src/analysis/))، لكن التحليل الدلالي الكامل في انتظار الاكتمال.
- **الميزات (مخططة):**
  - إدارة جداول الرموز والنطاقات.
  - حل الأسماء (ربط المعرفات بالإعلانات).
  - فحص الأنواع الشامل والاستنتاج.
  - تحليل تدفق التحكم (الوصول، مسارات الإرجاع).
  - تعليق AST بالمعلومات الدلالية.
  - *راجع [`docs/02_COMPILER_ARCHITECTURE/SEMANTIC_ANALYSIS.md`](SEMANTIC_ANALYSIS.md) و [`docs/04_ROADMAP/SEMANTIC_ANALYSIS_ROADMAP.md`](../04_ROADMAP/SEMANTIC_ANALYSIS_ROADMAP.md).*

### 9. توليد الكود 📋 مخطط

يحول AST (المحلل دلالياً) إلى كود تنفيذي أو تمثيل وسيط.

- **الحالة**: مخطط للتنفيذ المستقبلي. بدايات تكامل LLVM الأساسية ومنطق البناء الشرطي موجودة. توليد IR الفعلي من AST معلق.
- **الميزات (مخططة):**
  - توليد LLVM IR من AST المحلل دلالياً.
  - تمريرات التحسين، معلومات التصحيح، تخطيط المصدر.
  - دعم منصات هدف متعددة (x86-64، ARM64، Wasm).
  - *راجع [`docs/02_COMPILER_ARCHITECTURE/CODE_GENERATION.md`](CODE_GENERATION.md) و [`docs/04_ROADMAP/LLVM_CODEGEN_ROADMAP.md`](../04_ROADMAP/LLVM_CODEGEN_ROADMAP.md).*

## نظام البناء

نظام البناء يعتمد على CMake ويوفر:

- **إدارة المكونات**: كل مكون يُبنى كمكتبة ثابتة منفصلة.
- **تكامل الاختبار**: اختبار تلقائي لكل مكون (عبر CTest).
- **متعدد المنصات**: دعم أنظمة تشغيل متعددة.
- **التكوين**: خيارات تكوين بناء مرنة (مثل `USE_LLVM`).

## إطار الاختبار

بنية تحتية شاملة للاختبار:

- **اختبارات الوحدة**: اختبارات للمكونات الفردية ([`tests/unit/`](../../tests/unit/)).
- **اختبارات التكامل**: اختبارات لتفاعل المكونات (مخططة في [`tests/integration/`](../../tests/integration/)).
- **حالات اختبار عربية**: حالات اختبار مع إدخال عربي ([`tests/resources/`](../../tests/resources/)).
- **الإطار**: إطار اختبار بسيط قائم على التأكيدات في [`tests/framework/`](../../tests/framework/).

## معالجة الأخطاء

نظام شامل لمعالجة الأخطاء مع دعم محسّن للأخطاء اللفظية:

### معالجة الأخطاء اللفظية ✅ (منفذة بالكامل)
- **أنواع أخطاء محددة**: 8 أنواع رموز خطأ متميزة مع رموز خطأ فريدة
- **فئات الأخطاء**: منظمة حسب النوع (سلسلة نصية، هروب، حرف، رقم، تعليق، ذاكرة، عملية)
- **الترجمة العربية**: رسائل خطأ عربية كاملة واقتراحات قابلة للتنفيذ
- **سياق غني**: رموز الأخطاء، الفئات، الاقتراحات، وتتبع موقع المصدر المحسّن
- **استرداد الأخطاء**: آليات مزامنة قوية للمتابعة بعد الأخطاء
- **إدارة الذاكرة**: تنظيف مناسب لسياقات الأخطاء وهياكل الرموز المحسّنة

### أنواع الأخطاء الأخرى
- **أخطاء المعالج المسبق**: أخطاء تضمين الملفات، توسع الماكرو، التجميع الشرطي
- **أخطاء المحلل النحوي**: كشف الأخطاء النحوية، الإبلاغ عنها، واسترداد وضع الذعر الأساسي (مخطط)
- **الأخطاء الدلالية**: فحص الأنواع، أخطاء حل النطاقات (مخططة)
- **أخطاء توليد الكود**: أخطاء توليد الكود والتحسين (مخططة)

### ميزات الإبلاغ عن الأخطاء
- **موقع المصدر**: إبلاغ دقيق عن الأخطاء مع معلومات الملف، السطر، والعمود
- **امتدادات محسّنة**: مواقع البداية/النهاية وإزاحات الأحرف لتكامل أفضل مع IDE
- **رموز الأخطاء**: رموز عددية فريدة للترجمة ودعم الأدوات

## إدارة الذاكرة

استراتيجية إدارة الذاكرة:

- **التخصيص اليدوي**: يستخدم أغلفة مخصصة (`baa_malloc`، `baa_free`، `baa_strdup`) حول دوال المكتبة القياسية للتتبع أو الاستبدال المحتمل.
- **قواعد الملكية**: المكونات مسؤولة عن تحرير الذاكرة التي تخصصها (مثل المحلل اللفظي يخصص الرموز، المحلل النحوي/AST يدير عقد AST).

## التوسعات المستقبلية

التحسينات المخططة:

- التحليل الدلالي الشامل وتوليد الكود.
- تنفيذ المكتبة القياسية الكامل.
- تمريرات المُحسِّن.
- تكامل IDE (LSP).

## تدفق البيانات والتفاعل

### مرحلة المعالجة المسبقة

```
ملف_المصدر.ب
       ↓ [UTF-8/UTF-16 Detection]
   مجرى_خام
       ↓ [Directive Processing]
   تدفق_مُعالج (UTF-16LE)
```

### مرحلة التحليل اللفظي

```
تدفق_UTF-16LE
       ↓ [Character Classification]
   رموز_أولية
       ↓ [Keyword Recognition]
   رموز_مصنفة
       ↓ [Arabic Number Processing]
   تدفق_رموز_نهائي
```

### مرحلة التحليل النحوي

```
تدفق_الرموز
       ↓ [Recursive Descent Parsing]
   عقد_جزئية
       ↓ [AST Node Construction]
   شجرة_نحو_مجردة
       ↓ [Source Location Tracking]
   شجرة_نحو_موثقة
```

## إدارة الأخطاء والتشخيصات

### نظام التشخيصات

```c
typedef struct {
    BaaDiagnosticLevel level;    // خطأ، تحذير، ملاحظة
    BaaSourceLocation location; // موقع المشكلة
    const char* message_ar;     // الرسالة بالعربية
    const char* message_en;     // الرسالة بالإنجليزية
    BaaSourceRange context;     // السياق المحيط
} BaaDiagnostic;
```

### استراتيجيات الاسترداد

1. **المعالج المسبق**: تجاهل التوجيهات الخاطئة والمتابعة
2. **المحلل اللفظي**: تسجيل رموز خطأ والمتابعة
3. **المحلل النحوي**: مزامنة على نقاط استرداد معرفة
4. **التحليل الدلالي**: المتابعة بعد تسجيل الأخطاء

## التحسينات والأداء

### استراتيجيات الذاكرة

1. **تجميع الذاكرة**: استخدام مُخصصات مخصصة للعقد
2. **إعادة الاستخدام**: تجميع الكائنات المؤقتة
3. **التنظيف التلقائي**: تنظيف هرمي لشجرة النحو المجردة

### تحسينات الأداء

1. **تخزين مؤقت للرموز**: تخزين مؤقت لحالات البحث الشائعة
2. **تحليل تزايدي**: إعادة تحليل الأجزاء المتغيرة فقط
3. **المعالجة المتوازية**: معالجة ملفات متعددة بالتوازي

## الامتداد والتطوير

### إضافة ميزات لغوية جديدة

1. **تحديث المحلل اللفظي**: إضافة كلمات مفتاحية جديدة
2. **تمديد المحلل النحوي**: إضافة قواعد نحوية
3. **توسيع شجرة النحو المجردة**: تعريف أنواع عقد جديدة
4. **تحديث التحليل الدلالي**: إضافة قواعد تحقق

### إرشادات التطوير

1. **الاختبارات أولاً**: كتابة اختبارات قبل التنفيذ
2. **التوثيق المتزامن**: تحديث التوثيق مع الكود
3. **الواجهات الثابتة**: تجنب كسر الواجهات الموجودة
4. **المراجعة الثنائية**: مراجعة جميع التغييرات

## المبادئ المعمارية

### 1. الفصل بين الاهتمامات
- كل مكون له مسؤولية واضحة ومحددة
- التفاعل من خلال واجهات محددة بوضوح
- تجنب التبعيات الدائرية

### 2. قابلية الاختبار
- كل مكون قابل للاختبار بشكل منفصل
- حقن التبعيات حيث أمكن
- اختبارات شاملة لجميع الواجهات

### 3. قابلية الصيانة
- كود واضح ومؤثق
- أسماء ذات معنى للدوال والمتغيرات
- تجميع منطقي للوظائف ذات الصلة

### 4. الأداء
- تحسينات مبررة بقياسات فعلية
- تجنب التحسين المبكر
- الحفاظ على البساطة أولاً

---

**معمارية مترجم باء مصممة لتكون قوية وقابلة للصيانة مع دعم أصيل للغة العربية في جميع المستويات.**
