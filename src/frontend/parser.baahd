// عقد المحلل القواعدي المكتوب بباء.
// هذه الواجهة لا تغيّر المسار الإنتاجي بعد؛ `parse(Lexer*)` يبقى عقد C الخارجي.
// تستخدم مؤشرات خامة حتى لا يعتمد كود باء على تخطيط `Lexer` أو `Node` قبل تثبيت الغلاف.

#إذا_لم_يعرف حارس_محلل_قواعد_باء
#تعريف حارس_محلل_قواعد_باء 1

#تضمين "ast.baahd"

عقدة_باء محلل_قواعد_باء_حلل(ط٨* محلل_لفظي).

#نهاية
