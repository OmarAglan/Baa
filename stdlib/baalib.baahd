// واجهات مكتبة باء القياسية (v0.3.9)
// ملاحظة الملكية:
// - نسخ_نص ودمج_نص يعيدان نصاً مخصصاً على heap.
// - يجب تحرير النتيجة لاحقاً عبر حرر_نص.

صحيح طول_نص(نص س).
صحيح قارن_نص(نص أ، نص ب).
نص نسخ_نص(نص س).
نص دمج_نص(نص أ، نص ب).
عدم حرر_نص(نص س).

