// RUN: expect-pass runtime

صحيح الرئيسية() {
    نص أ = "باء".
    نص ب = "باء".
    نص ج = "لغة".

    إذا (طول_نص(أ) <= 0) {
        إرجع 1.
    }
    إذا (قارن_نص(أ، ب) != 0) {
        إرجع 2.
    }
    إذا (قارن_نص(أ، ج) == 0) {
        إرجع 3.
    }

    إرجع 0.
}
