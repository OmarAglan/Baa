#تضمين "tests/fixtures/multifile_visibility_api.baahd"

صحيح الرئيسية() {
    إذا (زيادة_وحدة_أ() != ١) { إرجع ١. }
    إذا (زيادة_وحدة_أ() != ٢) { إرجع ٢. }
    إذا (زيادة_وحدة_ب() != ١) { إرجع ٣. }
    إذا (زيادة_وحدة_ب() != ٢) { إرجع ٤. }
    إرجع ٠.
}
