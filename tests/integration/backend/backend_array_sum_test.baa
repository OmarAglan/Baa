صحيح الرئيسية() {
    صحيح قائمة[٥].
    صحيح مجموع = ٠.

    لكل (صحيح س = ٠؛ س < ٥؛ س = س + ١) {
        قائمة[س] = س * ١٠.
    }

    لكل (صحيح س = ٠؛ س < ٥؛ س = س + ١) {
        مجموع = مجموع + قائمة[س].
    }

    إذا (مجموع != ١٠٠) {
        إرجع ١.
    }

    إرجع ٠.
}

