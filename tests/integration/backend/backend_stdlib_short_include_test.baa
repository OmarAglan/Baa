// RUN: expect-pass runtime

#تضمين "baalib.baahd"

صحيح الرئيسية() {
    نص ن = دمج_نص("B", "aa").
    إذا (ن == عدم) {
        إرجع ١.
    }

    صحيح مطابق = (قارن_نص(ن، "Baa") == ٠).
    حرر_نص(ن).

    إذا (!مطابق) {
        إرجع ٢.
    }

    إرجع ٠.
}
