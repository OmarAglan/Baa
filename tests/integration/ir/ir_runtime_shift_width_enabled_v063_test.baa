// RUN: expect-pass,compile-only
// FLAGS: -S -fruntime-checks
// EXPECT-ASM: فشل_إزاحة_غير_صالحة

صحيح أربعة_وستون() {
    إرجع ٦٤.
}

صحيح الرئيسية() {
    صحيح ن = أربعة_وستون().
    صحيح س = ١ << ن.
    إرجع س.
}
