// RUN: compile-only
// FLAGS: -S
// EXPECT-ASM: rdtsc
// EXPECT-ASM: nop

صحيح قراءة_عداد() {
    إرجع اقرأ_عداد_الزمن().
}

صحيح الرئيسية() {
    لا_تفعل().

    صحيح قيمة = قراءة_عداد().
    إذا (قيمة < ٠) {
        إرجع ١.
    }
    إرجع ٠.
}
